#lang slideshow

(slide (t "using akka")
       (item "If you want to create an example, you"
             "can always do something with" (bt "Hello World!"))
       (item "First step")
       'next
       (item "Second step"))

(slide (t "why akka")
 (item "Tentative third step")
 'next
 (item "This isn't working... back up")
 'next
 (item "Third step that works"))
